# Project exclude paths
/.idea/
/out/
/saves/
/lib/
*.iml
/data/JetBrainsMono-2.242
/processing-3.5.4/

src/test/Camera.java
/.fleet/

# save and screenshot folder, let's not clog the repo with huge png
/data/gui/
/library/
/deploy/
/deploy_resources/
/deploy_resources/*
/target/
.project
.classpath
/.settings/
/data/recording_assets/
/auto_deploy/

# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build

# Signing key material & publishing credentials — never commit
# (secret keys normally live in ~/.gnupg and the Central token in ~/.gradle/gradle.properties, both outside the repo)
*.gpg
*.asc
*.key
secring.gpg
