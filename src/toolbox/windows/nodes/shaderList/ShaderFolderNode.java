package toolbox.windows.nodes.shaderList;

import toolbox.windows.nodes.FolderNode;

public class ShaderFolderNode extends FolderNode {

    public ShaderFolderNode(String path, FolderNode parent) {
        super(path, parent);
    }
}
