package toolbox.windows;

import processing.core.PVector;
import toolbox.tree.nodes.FolderNode;

public class ColorPickerWindow extends FolderWindow{
    public ColorPickerWindow(PVector pos, PVector size, FolderNode folder, boolean closeable) {
        super(pos, size, folder, closeable);
    }


}
